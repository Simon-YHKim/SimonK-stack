# Spec: <기능명>

- 작성일: YYYY-MM-DD
- 상태: draft
- slug: <kebab-slug>
- 축(해당 시): <어느 제품 축/영역에 속하는가>

> 한 장으로 끝낸다. 구현자가 5분 안에 읽고 시작할 수 있어야 한다.
> 모르는 칸은 추정으로 채우지 말고 `OPEN:` 으로 남긴다.

## 1. 문제

<지금 무엇이 불편한가. 이 기능이 없으면 무슨 일이 일어나는가. 1~3문장.>

## 2. 대상 사용자

- 1차 사용자: <누가, 어떤 상황에서>
- 2차 사용자: <있으면, 없으면 N/A>
- 권한 등급: <비로그인 / 일반 / 관리자 등 — 권한 분기가 있으면 명시>

## 3. 성공 기준

> 이 기능이 "됐다"고 말하려면 무엇이 관찰돼야 하는가. 측정 가능한 지표 1~3개.

- <예: 사용자가 3탭 안에 항목을 저장한다>
- <예: 재방문 시 저장한 항목이 그대로 보인다>

## 4. 범위

### In Scope

> 이번에 만드는 것 (구체 동작 목록).

- <동작 1>
- <동작 2>

### Out of Scope

> 이번에 **안** 만드는 것. 다음으로 미루는 것. (비워두지 말 것 — scope creep 차단)

- <제외 1>
- <제외 2>

## 5. 엣지 케이스

> 해당 없으면 N/A 로 명시. 모르면 `OPEN:` 으로.

| 상황 | 기대 동작 |
|---|---|
| 빈 입력 / null / 길이 0 | <> |
| 최대치 (긴 입력, 큰 목록, 동시 요청) | <> |
| 네트워크 끊김 / 타임아웃 / 재시도 | <> |
| 권한 거부 (비로그인, 타인 리소스 — IDOR) | <> |
| 동시성 / 경쟁 상태 | <> |
| 기존 데이터 마이그레이션 | <> |
| 플랫폼 차이 (iOS / Android / Web) — RN·웹일 때 | <> |

## 6. 수용 기준 (Given-When-Then)

> 테스트의 씨앗. 형용사 금지, 시나리오로. 3~7개.

1. **Given** <상태> **When** <행동> **Then** <관찰 가능한 결과>
2. **Given** <> **When** <> **Then** <>
3. **Given** <> **When** <> **Then** <>

## 7. 의존

- API / 엔드포인트: <>
- 테이블 / 스키마: <>
- 외부 서비스: <>
- secret: <코드에 박지 않음. 서버 경유 / env 키 이름만 — 값은 절대 적지 않음>
- i18n: <사용자 노출 문자열 EN/KO 키 필요 여부>

## 8. 리스크 & 롤백

- 깨질 수 있는 것: <>
- 롤백 방법: <이 기능을 끄거나 되돌리려면>
- 관측 (observability): <프로덕션에서 깨진 걸 어떻게 알아채나>

## 9. 미해결 질문 (OPEN)

> 해소 전까지 상태는 `draft`. 모두 지워지면 `ready`.

- OPEN: <아직 답이 없는 결정 1>
- OPEN: <아직 답이 없는 결정 2>
